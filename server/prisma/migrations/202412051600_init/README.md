# 202412051600_init

Initial migration capturing the CRM data structures (Accounts, Contacts,
Activities, Movements, Opportunities) so Prisma can materialize the schema on
fresh databases without touching existing prod data.

